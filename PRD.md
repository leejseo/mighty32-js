# Mighty32 Lite PRD

## 목적

혼자서 짧게 즐길 수 있는 브라우저 기반 마이티 웹게임을 만든다. 로컬 Node 서버로 실행하면 게임 결과를 레포 안의 `data/games.jsonl`에 누적하고, 서버 없이 열 때는 브라우저 저장소로 대체한다. UI는 옛날 Windows 9x/마이티32 느낌의 가벼운 스타일을 제공한다.

## 대상 사용자

- 마이티 룰을 대략 알고 있고 혼자 빠르게 한 판 돌리고 싶은 사용자
- 복잡한 계정, 매칭, 설치 없이 브라우저에서 바로 플레이하고 싶은 사용자
- 옛날 PC 카드게임 UI의 밀도와 감성을 선호하는 사용자

## 핵심 요구사항

- 5인 마이티 흐름을 1인 대 4 CPU로 플레이한다.
- 입찰, 주공 결정, 바닥패 확인 후 공약 조정, 프렌드 선택, 바닥패 묻기, 10트릭 진행, 점수 계산, 결과 저장까지 한 판이 완결된다.
- CPU는 입찰, 프렌드 선택, 카드 버리기, 카드 플레이를 자동으로 수행한다. 입찰은 판마다 조금씩 달라지는 성향을 반영하되 기본적으로 보수적이며, 16점 이상은 강한 기루 모양과 조커/마이티 보조 컨트롤이 있을 때만 시도한다. 17점 이상은 매우 드문 확신 입찰로 취급한다. 프렌드는 핵심 컨트롤 선택에서 무작위성을 줄이고, 플레이 중에는 프렌드가 무리하게 마이티/조커를 선공으로 쓰기보다 낮은 기루다 반환과 낮은 카드 탐색을 우선한다.
- CPU 이름은 마이티32 감성에 맞춰 이명박, 김영삼, 김대중, 최규하로 표시한다.
- CPU 기본 초상은 생성형 멀티모달 모델로 만든 2D 카드게임풍 PNG 애셋을 사용한다.
- 사용자는 CPU 캐릭터의 이름과 이미지 URL/경로를 수정하고 로컬 저장소에 저장할 수 있다.
- 사용자는 자신의 손패만 보고 조작한다.
- 최근 게임 기록은 로컬 서버 실행 시 `data/games.jsonl`에 저장한다. 파일 저장 API가 없으면 IndexedDB, 그마저 사용할 수 없으면 localStorage로 대체한다.
- GitHub Pages 정적 호스팅에서는 기록 저장을 비활성화한다.
- UI는 회색 패널, 파란 타이틀바, 픽셀풍 버튼, 초록색 테이블 중심의 레트로 데스크톱 스타일을 사용한다.

## 라이트 룰 범위

정식 마이티의 모든 지역 룰을 구현하기보다 빠른 싱글 플레이에 필요한 핵심만 반영한다.

- 덱: 52장 + 조커 1장
- 플레이어: 5명, 각 10장, 바닥패 3장
- 점수 카드: A, K, Q, J, 10 총 20점
- 입찰: 일반 기루는 13점부터 20점까지, 노기루는 12점부터 20점까지 가능하다. 기존 입찰보다 높은 점수로만 입찰할 수 있으며, 같은 점수에서는 노기루가 일반 기루보다 높다.
- 주공: 최고 입찰자
- 기루: 입찰 시 선택한 문양 또는 노기루
- 마이티: 기본은 스페이드 A, 기루가 스페이드면 다이아 A
- 노기루: 기루 문양 보정 없이 진행하며 마이티는 스페이드 A
- 카드 서열: 마이티 > 조커 > 기루 문양 > 리드 문양 > 기타
- 공약 조정: 주공은 바닥패 3장을 받은 뒤 기존 공약을 유지하거나, 기존 입찰보다 높은 공약으로 목표/기루를 조정할 수 있다.
- 프렌드: 주공이 자신에게 없는 카드를 지정한다. 해당 카드를 낸 플레이어가 공개 프렌드가 된다. CPU 주공은 조커/마이티, 기루 강패, 주공이 비거나 짧은 문양의 A/K, 목표 점수 압박을 고려해 프렌드를 고른다. 조커와 마이티가 모두 없을 때는 조커보다 마이티 프렌을 우선하는 쪽으로 평가한다. CPU 주공의 프렌드 선택은 플레이 시작 전에 한 번 상태 메시지로 표시한다.
- 묻기: 주공은 바닥패 3장을 가져간 뒤 3장을 묻는다. 묻은 점수 카드는 주공 팀 점수에 포함한다. 사용자와 CPU 모두 주공이 아니면 라운드 종료 전까지 바닥 점수를 알 수 없다.
- 딜미스: 초기 손패에 점수 카드가 0장이면 딜미스이다. 사람은 재딜 버튼을 누르고, CPU 딜미스는 자동 재딜한다.
- 첫 트릭 리드 제한: 주공은 일반 카드가 있으면 첫 트릭에 기루, 마이티, 조커, 조커콜 선언을 리드할 수 없다. 조커콜 카드를 일반 카드로 내는 것은 허용한다.
- 팔로우: 리드 문양을 보유하면 따라야 한다. 마이티가 선으로 나오면 스페이드 리드로 취급해 스페이드 보유자는 스페이드를 내야 한다. 마이티와 조커는 일반 팔로우에서는 언제든 낼 수 있다. 조커는 리드할 때 문양을 지정하며, 첫 트릭과 마지막 트릭에는 최약 카드가 된다.
- 조커콜: 조커콜 카드를 선으로 낼 때 호출 여부를 선택한다. 조커콜로 선언된 리드에서만 조커 보유자는 조커를 내야 하며, 마이티로 대신 받을 수 있다.
- 규칙 위반 방지: CPU는 합법 카드 목록에서만 선택하고, 실제 카드 제출 함수도 불법 플레이를 거부한다.
- 성공 조건: 주공 팀 점수가 입찰 목표 이상이면 성공

## 주요 화면

- 게임 테이블: 5명 좌석, 상대 2D 초상, 상대 이름표의 남은 장수, 중앙 제출 카드 자리, 내 손패
- 진행 패널: 입찰 컨트롤, 프렌드 선택, 묻기 확인, 결과 액션
- 판 정보 패널: 주공, 기루, 목표, 주공 팀 공개 점수, 프렌드 상태, 바닥 점수 공개/비공개 상태
- 기록 패널: 최근 전적, 승패, 주공, 목표, 점수
- 로그 패널: 입찰, 프렌드 공개, 트릭 획득, 결과
- 캐릭터 패널: CPU 이름과 이미지 URL/경로 수정, 기본값 초기화

## 저장 데이터

게임 종료 시 다음 값을 저장한다.

기본 실행 경로는 `node server.js`이며, 완료 기록은 레포 안의 `data/games.jsonl`에 JSON Lines 형식으로 누적한다.

- 종료 시각
- 주공 이름과 인덱스
- 사용자가 주공이었는지 여부
- 프렌드 플레이어와 프렌드 카드
- 기루
- 목표 점수
- 주공 팀 획득 점수
- 주공 팀 성공 여부
- 사용자 편 승리 여부

## 비목표

- 온라인 멀티플레이
- 계정/서버 저장
- 지역 룰별 세부 옵션 전체 구현
- 애니메이션 중심의 고사양 UI
- 완전한 CPU 전략 AI. 단, CPU는 성향 기반 입찰 평가, 바닥패 확인 후 공약 조정, 기루 A/K와 장수 기반의 입찰 상한, 프렌드 후보 점수화, 안전한 점수 묻기, 조커콜 선언 여부, 조커 효력 타이밍, 현재 트릭 승자/아군 여부/점수 카드, 후보 카드별 현재 트릭 시뮬레이션에 기반한 전략을 수행한다. 프렌드로 판단된 CPU는 초중반에 보통 기루다가 있으면 낮은 기루다 반환을 우선하고, 특별한 점수 회수 명분 없이 마이티/조커를 선공으로 쓰는 선택을 강하게 억제한다. 주공은 낮은 기루다로 장악 카드를 뽑는 리드를 고려하고, 수비는 적 조커를 조커콜로 끌어내는 선택과 아군 트릭을 비싼 카드로 뺏지 않는 선택을 평가한다. 마이티는 자체 점수 1점과 별도로 보존 가치를 강하게 평가해, 초중반 빈 트릭이나 아군이 이미 먹는 트릭에는 쓰지 않도록 한다. 단, 높은 공약이나 남은 트릭 대비 필요 점수가 큰 상황에서는 1점 차이도 치명적일 수 있으므로 마이티 보존 감점을 완화한다. AI는 현재까지 공개된 플레이 기록을 바탕으로 문양 보이드와 공개적으로 남은 카드 수를 추정해 리드 위험도를 조정한다. 현재 트릭 안에서는 남은 응답을 완전정보 근사로 알파베타 탐색해 후보 카드 평가에 반영한다.

### 장기 AI 확장

- 현재 구현은 즉시 적용 가능한 범위로 현재 트릭의 알파베타 탐색을 사용한다. 남은 패 전체를 대상으로 한 장기 탐색은 상대 손패가 불완전정보이므로 단순 minimax보다 추정 샘플을 여러 개 만든 뒤 각 샘플에서 alpha-beta 또는 MCTS를 돌려 평균/분산을 보는 방식이 적합하다.
- 확장 순서는 1) 공개 정보로 가능한 상대 패 분포 생성, 2) 프렌드 정체와 묻힌 카드에 대한 관측 제한 유지, 3) 후보 카드 상위 N개만 장기 rollout, 4) 입찰/프렌드 선택에도 같은 평가 함수를 재사용하는 방향이 안전하다.

## 완료 기준

- `index.html`을 열면 바로 게임이 시작된다.
- 사용자가 입찰하거나 패스해서 한 판을 끝낼 수 있다.
- CPU 주공 판도 자동으로 진행된다.
- 사용자가 주공이면 프렌드 선택과 3장 묻기 후 플레이가 진행된다.
- 게임 종료 후 최근 기록이 UI에 표시되고 새로고침 후에도 유지된다.
